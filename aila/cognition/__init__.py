"""Cognição da Aila — memória cognitiva + grafos (Knowledge Graph, Code Graph).

Subsistema ADITIVO (Fase 1+). Não substitui nada: a memória atual (aila/memory)
continua funcionando; aqui evoluímos o modelo de dados e, nas próximas fases,
o retrieval híbrido, a consolidação e os grafos. Tudo local-first (SQLite +
NetworkX, CPU/RAM — não VRAM).
"""
